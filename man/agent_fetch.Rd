% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-agent-fetch.R
\name{agent_fetch}
\alias{agent_fetch}
\title{Fetch a CVM open-data table for a registered-agent dataset}
\usage{
agent_fetch(
  dataset,
  table,
  agent = NULL,
  as_of = NULL,
  source = NULL,
  on_error = "abort",
  validate = "strict",
  ...
)
}
\arguments{
\item{dataset}{Short dataset id (e.g. \code{"administradores"},
\code{"agentes-autonomos"}).}

\item{table}{Snake-case table name within the dataset.}

\item{agent}{Optional character vector identifying registered
agents to include. Accepts CPF, CNPJ, or free-text — the
accepted token types vary by dataset:
\itemize{
\item \code{agentes-autonomos} accepts CPF only;
\item \code{coordenadores-de-ofertas} accepts CNPJ only;
\item \code{administradores}, \code{auditores},
\code{consultores-de-valores-mobiliarios} accept CPF, CNPJ and
free-text against the registered name.
Validation is performed against the target dataset at fetch
time. \code{NULL} (default) returns every registered agent.
Singular naming follows tidyverse conventions; the argument
still accepts vectors of any length.
}}

\item{as_of}{Optional \code{Date} scalar specifying a registry
snapshot date. \code{NULL} (default) returns the most recent
registry snapshot. For historical snapshots, pass a specific
date; the function returns the registry as published at the
closest available snapshot on or before that date.}

\item{source}{One of \code{"mirror"} (parquet via DuckDB) or \code{"cvm"}
(CVM Open Data Portal). \code{NULL} (default) resolves to the
active backend via \code{\link[=cvm_source_get]{cvm_source_get()}}.}

\item{on_error}{One of \code{"abort"} (default), \code{"warn"} or
\code{"silent"}. Controls behaviour on HTTP failures, analogously
to \code{\link[=issuer_fetch]{issuer_fetch()}}.}

\item{validate}{One of \code{"strict"} (default), \code{"warn"} or
\code{"skip"}. Controls schema validation strictness at parse time.}

\item{...}{Reserved for forward compatibility. Currently no
extra arguments are accepted; passing any aborts with
\code{cvmdata_error_input}.}
}
\value{
A tibble of class \code{cvm_tbl} carrying the five provenance
attributes \code{source}, \code{fetched_at}, \code{dataset}, \code{table} and
\code{package_version}.
}
\description{
Single entry point for retrieving any table from any CVM
agent-class dataset covered by the package. Covers natural and
legal persons registered with CVM across eight CKAN groups:
\code{administradores}, \code{agentes-autonomos}, \code{agentes-fiduciarios},
\code{auditores}, \code{consultores-de-valores-mobiliarios},
\code{coordenadores-de-ofertas}, \code{participantes-intermediarios} and
\code{investidores-nao-residentes}.
}
\details{
Column names, categorical values and free-text fields are
preserved in Portuguese exactly as published by CVM
(snake_case minúsculo).

In v0.1.0.9000 the function is exported as a skeleton documenting
the planned contract; calling it aborts with
\code{cvmdata_error_input_group} pointing to ROADMAP.md. Full
implementation arrives in v0.7.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# Available from v0.7 onwards:
admins <- agent_fetch("administradores", "cadastro")

# Snapshot at a historical date:
aa <- agent_fetch("agentes-autonomos", "cadastro",
                  as_of = as.Date("2023-12-31"))
\dontshow{\}) # examplesIf}
}
\seealso{
Other fetchers:
\code{\link[=event_fetch]{event_fetch()}},
\code{\link[=fund_fetch]{fund_fetch()}},
\code{\link[=issuer_fetch]{issuer_fetch()}},
\code{\link[=offering_fetch]{offering_fetch()}}
}
\concept{fetchers}
