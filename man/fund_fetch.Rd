% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-fund-fetch.R
\name{fund_fetch}
\alias{fund_fetch}
\title{Fetch a CVM open-data table for a fund dataset}
\usage{
fund_fetch(
  dataset,
  table,
  fund = NULL,
  date = NULL,
  source = NULL,
  on_error = "abort",
  validate = "strict",
  ...
)
}
\arguments{
\item{dataset}{Short dataset id (e.g. \code{"fi-cad"},
\code{"informe-diario"}, \code{"cda"}).}

\item{table}{Snake-case table name within the dataset.}

\item{fund}{Optional character vector of fund CNPJs to include.
\code{NULL} (default) returns every fund. Free-text matching against
fund denomination is \strong{not} supported in the current API
because CVM Resolution 175 (2023) restructured fund nomenclature
into Classe / Subclasse / Razão Social columns that change
between issues; CNPJ remains the stable identifier. Free-text
matching may be added in v0.5+ if a stable column emerges.
Singular naming follows tidyverse conventions; the argument
still accepts vectors of any length.}

\item{date}{Optional \code{Date} vector of reference dates. \code{NULL}
(default) fetches the latest available month or quarter.
Reference dates are point-in-time (end-of-month for monthly
reports, last business day of quarter for quarterly). Singular
naming; the argument accepts vectors of any length. Pass
\code{seq.Date()} for ranges or specific historical points.}

\item{source}{One of \code{"mirror"} (parquet via DuckDB) or \code{"cvm"}
(CVM Open Data Portal). \code{NULL} (default) resolves to the
active backend via \code{\link[=cvm_source_get]{cvm_source_get()}}.}

\item{on_error}{One of \code{"abort"} (default), \code{"warn"} or
\code{"silent"}. Controls behaviour on HTTP failures for batches of
dated tables, analogously to \code{\link[=issuer_fetch]{issuer_fetch()}}.}

\item{validate}{One of \code{"strict"} (default), \code{"warn"} or
\code{"skip"}. Controls schema validation strictness at parse time.}

\item{...}{Reserved for forward compatibility. Currently no
extra arguments are accepted; passing any aborts with
\code{cvmdata_error_input}.}
}
\value{
A tibble of class \code{cvm_tbl} carrying the five provenance
attributes \code{source}, \code{fetched_at}, \code{dataset}, \code{table} and
\code{package_version}.
}
\description{
Single entry point for retrieving any table from any CVM
fund-class dataset covered by the package. Covers investment
funds across three CKAN groups: \code{fundos-de-investimento}
(ICVM 555 funds), \code{fundos-de-investimento-imobiliarios} (FII)
and \code{fundos-estruturados} (FIP, FIDC, FAPI, etc.).
}
\details{
Column names, categorical values and free-text fields are
preserved in Portuguese exactly as published by CVM
(snake_case minúsculo).

In v0.1.0.9000 the function is exported as a skeleton documenting
the planned contract; calling it aborts with
\code{cvmdata_error_input_group} pointing to ROADMAP.md. Full
implementation arrives in v0.4 (\code{fundos-de-investimento}), v0.5
(\code{fundos-de-investimento-imobiliarios}) and v0.6
(\code{fundos-estruturados}).
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# Available from v0.4 onwards (fundos-de-investimento):
funds <- fund_fetch("fi-cad", "cad",
                    fund = "12.345.678/0001-90")

# Available from v0.5 onwards (fundos-de-investimento-imobiliarios):
fii <- fund_fetch("fii", "informe-mensal",
                  date = as.Date("2024-03-31"))
\dontshow{\}) # examplesIf}
}
\seealso{
Other fetchers:
\code{\link[=agent_fetch]{agent_fetch()}},
\code{\link[=event_fetch]{event_fetch()}},
\code{\link[=issuer_fetch]{issuer_fetch()}},
\code{\link[=offering_fetch]{offering_fetch()}}
}
\concept{fetchers}
