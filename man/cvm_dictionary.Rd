% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{cvm_dictionary}
\alias{cvm_dictionary}
\title{Look up the CVM-published dictionary for a table}
\usage{
cvm_dictionary(dataset, table, group = NULL)
}
\arguments{
\item{dataset}{Short dataset id (e.g. \code{"dfp"}).}

\item{table}{Table name within the dataset (e.g. \code{"bpa"}).}

\item{group}{Optional CKAN group slug (e.g. \code{"companhias"}). When
\code{NULL} (default) the function resolves by uniqueness across the
embedded snapshot. From v0.4 onward, datasets may exist in more
than one group; in that case omitting \code{group} aborts with
\code{cvmdata_error_input_ambiguous}.}
}
\value{
A tibble with one row per column of the table, columns
\code{campo} (snake-case name matching \code{\link[=issuer_fetch]{issuer_fetch()}} output),
\code{campo_original} (field name as published by CVM in the META,
preserving the original casing), \code{descricao}, \code{dominio},
\code{tipo_dados}, \code{tamanho}, \code{precisao}, \code{scale}.
}
\description{
Returns the dictionary metadata (field names, descriptions, domain,
data type, size/precision/scale) for a single \verb{(dataset, table)}
pair, sourced from the snapshot embedded under
\code{inst/extdata/cvm_dictionary_snapshot.csv}. The snapshot is built
offline from the CVM META resources by
\code{data-raw/build-dictionary-snapshot.R}.
}
\details{
Tables whose META is not published by CVM (\code{meta_status: missing} in
the schema YAML) return rows with \code{NA} in every metadata column
except \code{campo}/\code{campo_original} (sourced from the YAML's
\code{expected_field_names}). In that case the returned tibble carries an
attribute \code{meta_status = "missing"}.
}
\examples{
cvm_dictionary("dfp", "bpa")
cvm_dictionary("fre", "empregado_PCD")
}
\seealso{
\code{\link[=cvm_tables]{cvm_tables()}}

Other discovery:
\code{\link[=cvm_codelist]{cvm_codelist()}},
\code{\link[=cvm_dataset_years]{cvm_dataset_years()}},
\code{\link[=cvm_datasets]{cvm_datasets()}},
\code{\link[=cvm_groups]{cvm_groups()}},
\code{\link[=cvm_tables]{cvm_tables()}}
}
\concept{discovery}
