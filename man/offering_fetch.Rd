% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-offering-fetch.R
\name{offering_fetch}
\alias{offering_fetch}
\title{Fetch a CVM open-data table for a public-offering dataset}
\usage{
offering_fetch(
  dataset,
  table,
  offering = NULL,
  date_range = NULL,
  source = NULL,
  on_error = "abort",
  validate = "strict",
  ...
)
}
\arguments{
\item{dataset}{Short dataset id (e.g. \code{"ofertas-publicas"},
\code{"crowdfunding"}).}

\item{table}{Snake-case table name within the dataset.}

\item{offering}{Optional character vector of offering identifiers
(\code{numero_oferta}). \code{NULL} (default) returns every offering in
the date range. Covers both registered public offerings
(\code{ofertas-publicas}) and crowdfunding-platform offerings
(\code{plataformas-de-crowdfunding}, CVM Resolution 88). Singular
naming follows tidyverse conventions; the argument still
accepts vectors of any length.}

\item{date_range}{Optional \code{Date} vector of length 2 specifying
the offering date window: \code{c(from, to)}. \code{NULL} (default)
returns every offering ever published. Vectors of length other
than 2 abort with \code{cvmdata_error_input}.}

\item{source}{One of \code{"mirror"} (parquet via DuckDB) or \code{"cvm"}
(CVM Open Data Portal). \code{NULL} (default) resolves to the
active backend via \code{\link[=cvm_source_get]{cvm_source_get()}}.}

\item{on_error}{One of \code{"abort"} (default), \code{"warn"} or
\code{"silent"}. Controls behaviour on HTTP failures, analogously
to \code{\link[=issuer_fetch]{issuer_fetch()}}.}

\item{validate}{One of \code{"strict"} (default), \code{"warn"} or
\code{"skip"}. Controls schema validation strictness at parse time.}

\item{...}{Reserved for forward compatibility. Currently no
extra arguments are accepted; passing any aborts with
\code{cvmdata_error_input}.}
}
\value{
A tibble of class \code{cvm_tbl} carrying the five provenance
attributes \code{source}, \code{fetched_at}, \code{dataset}, \code{table} and
\code{package_version}.
}
\description{
Single entry point for retrieving any table from any CVM
offering-class dataset covered by the package. Covers both
registered public offerings (\code{ofertas-publicas}) and
crowdfunding-platform offerings under CVM Resolution 88
(\code{plataformas-de-crowdfunding}).
}
\details{
Column names, categorical values and free-text fields are
preserved in Portuguese exactly as published by CVM
(snake_case minúsculo).

In v0.1.0.9000 the function is exported as a skeleton documenting
the planned contract; calling it aborts with
\code{cvmdata_error_input_group} pointing to ROADMAP.md. Full
implementation arrives in v0.7.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# Available from v0.7 onwards (ofertas-publicas):
ipos <- offering_fetch("ofertas-publicas", "registradas",
                       date_range = as.Date(
                         c("2024-01-01", "2024-12-31")
                       ))

# Crowdfunding under CVM Resolution 88:
cf <- offering_fetch("crowdfunding", "ofertas")
\dontshow{\}) # examplesIf}
}
\seealso{
Other fetchers:
\code{\link[=agent_fetch]{agent_fetch()}},
\code{\link[=event_fetch]{event_fetch()}},
\code{\link[=fund_fetch]{fund_fetch()}},
\code{\link[=issuer_fetch]{issuer_fetch()}}
}
\concept{fetchers}
