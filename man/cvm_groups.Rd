% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-cvm-groups.R
\name{cvm_groups}
\alias{cvm_groups}
\title{List the CVM CKAN groups}
\usage{
cvm_groups()
}
\value{
A tibble with 18 rows, sorted alphabetically by \code{group}.
Columns:
\itemize{
\item \code{group} (character) -- the CKAN slug (e.g. \code{"companhias"},
\code{"fundos-de-investimento"}).
\item \code{n_datasets} (integer) -- count of datasets the CVM portal
publishes under this group.
\item \code{contract} (character) -- one of \code{"issuer"}, \code{"fund"}, \code{"agent"},
\code{"offering"}, \code{"event"}. Identifies the \code{cvmdata} fetcher that
covers (or will cover) the group.
}
}
\description{
Returns the 18 thematic groups published by the CVM Open Data Portal
(\verb{<https://dados.cvm.gov.br/group/>}), each with the number of
datasets the portal exposes for it and the canonical \code{cvmdata}
fetcher contract that covers it.
}
\details{
v0.1.0.9000 implements only the \code{companhias} group (via
\code{\link[=issuer_fetch]{issuer_fetch()}}); the remaining 17 rows document the planned
v0.2-v0.8 coverage so users can navigate the universe of CVM data
before the corresponding fetchers ship. Calls to the four
non-\code{issuer} fetchers in v0.1.0.9000 abort with
\code{cvmdata_error_input_group}; see \code{\link[=fund_fetch]{fund_fetch()}}, \code{\link[=agent_fetch]{agent_fetch()}},
\code{\link[=offering_fetch]{offering_fetch()}} and \code{\link[=event_fetch]{event_fetch()}}.

The \code{n_datasets} column reports the totals published by CVM, not the
count currently implemented in the package. The mapping between
groups and contracts is fixed for the lifetime of v0.1.x; see
\code{data-raw/decisions/cvmdata_arquitetura_grupos_decisao_v2.md} for the
rationale.
}
\examples{
cvm_groups()

# All groups served by issuer_fetch()
g <- cvm_groups()
g[g$contract == "issuer", ]
}
\seealso{
\code{\link[=cvm_datasets]{cvm_datasets()}}, \code{\link[=issuer_fetch]{issuer_fetch()}}

Other discovery:
\code{\link[=cvm_codelist]{cvm_codelist()}},
\code{\link[=cvm_dataset_years]{cvm_dataset_years()}},
\code{\link[=cvm_datasets]{cvm_datasets()}},
\code{\link[=cvm_dictionary]{cvm_dictionary()}},
\code{\link[=cvm_tables]{cvm_tables()}}
}
\concept{discovery}
