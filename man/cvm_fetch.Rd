% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-cvm-fetch.R
\name{cvm_fetch}
\alias{cvm_fetch}
\title{Fetch a CVM open-data table}
\usage{
cvm_fetch(
  dataset,
  table,
  companies = NULL,
  years = NULL,
  source = NULL,
  report_type = NULL,
  on_error = "abort",
  validate = "strict",
  ...
)
}
\arguments{
\item{dataset}{Short dataset id (e.g. \code{"cad"}, \code{"dfp"}).}

\item{table}{Snake-case table name (e.g. \code{"companhias"}, \code{"bpa"}).
For datasets with conceptually paired tables (individual /
consolidated), \code{table} is the concept name and \code{report_type}
selects the variant.}

\item{companies}{Optional character vector identifying companies to
include. Accepts CNPJ (with or without punctuation), CD_CVM (with
or without zero-padding), or free-text matched against
\code{denom_cia}. Detection is automatic per element. \code{NULL} (default)
returns every company. When the target table does not carry a
\code{cd_cvm} column (e.g. \code{composicao_capital}, \code{parecer}), CD_CVM
tokens are resolved to CNPJ via the dataset's \code{submissao} table
for the same year — the user-facing interface is identical
regardless of which table holds CD_CVM natively.}

\item{years}{Integer vector of years to fetch. \code{NULL} (default)
fetches the latest available year. Ignored for datasets with
\code{temporal_partitioning: none} (e.g. CAD).}

\item{source}{One of \code{"cvm"} (CVM Open Data Portal) or \code{"mirror"}
(parquet via DuckDB; available from Phase F of the roadmap).
\code{NULL} (default) resolves to the active backend via
\code{\link[=cvm_source_get]{cvm_source_get()}} — \code{"cvm"} in the v0.1 series unless the user
has flipped it with \code{\link[=cvm_source_set]{cvm_source_set()}}.}

\item{report_type}{One of \code{"ind"}, \code{"con"}, or \code{NULL}. Required for
tables that publish individual and consolidated variants
(\code{bpa}, \code{bpp}, \code{dre}, \code{dra}, \code{dfc_md}, \code{dfc_mi}, \code{dmpl}, \code{dva}).
Must be \code{NULL} for tables without that distinction
(\code{composicao_capital}, \code{submissao}, \code{parecer}, \code{companhias}).}

\item{on_error}{One of \code{"abort"} (default), \code{"warn"} or \code{"silent"}.
Controls behaviour on HTTP failures for batches of yearly tables
(\code{years = c(...)} with more than one element). With \code{"warn"}, the
failed years are skipped and a \code{cvmdata_warn_partial_failure}
warning lists them; with \code{"silent"}, the failed years are skipped
silently; with \code{"abort"}, the first failure aborts the call. Total
batch failure (every year fails) always aborts regardless of the
setting — there is no partial result to return. Parse/validation
failures are governed by \code{validate}, not \code{on_error}. Single-year
calls, non-yearly tables, and the implicit fallback when
\code{years = NULL} always abort on HTTP failure.}

\item{validate}{One of \code{"strict"} (default), \code{"warn"} or \code{"skip"}.
Controls schema validation strictness at parse time.}

\item{...}{Reserved for forward compatibility.}
}
\value{
A tibble of class \code{cvm_tbl} carrying the five provenance
attributes \code{source}, \code{fetched_at}, \code{dataset}, \code{table} and
\code{package_version}.
}
\description{
Single entry point for retrieving any table from any CVM dataset
covered by the package. Selects the right URL, downloads (or serves
from cache), parses, applies schema-declared transformations, and
returns a tibble carrying provenance attributes.
}
\details{
Column names, categorical values and free-text fields are preserved
in Portuguese exactly as published by CVM (snake_case minúsculo).
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# CAD: single snapshot, no temporal partitioning, no report_type
companies <- cvm_fetch("cad", "companhias")

# DFP BPA individual, latest year, single company
bb <- cvm_fetch("dfp", "bpa",
                report_type = "ind",
                companies = "BCO BRASIL",
                years = 2024)
\dontshow{\}) # examplesIf}
}
\seealso{
Other fetchers: 
\code{\link{cad_fetch}()}
}
\concept{fetchers}
