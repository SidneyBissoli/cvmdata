% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util-cnpj.R
\name{cnpj_format}
\alias{cnpj_format}
\title{Format a CNPJ vector with the canonical punctuation}
\usage{
cnpj_format(x)
}
\arguments{
\item{x}{Character vector of CNPJs (digits-only or punctuated).}
}
\value{
Character vector of the same length as \code{x}, formatted as
\code{"NN.NNN.NNN/NNNN-NN"}. \code{NA_character_} where the input is NA
or has a non-14-digit body.
}
\description{
Inverse of \code{\link[=cnpj_clean]{cnpj_clean()}}. Takes one or more CNPJs in any format
(digits-only or already punctuated) and returns them as
\code{"NN.NNN.NNN/NNNN-NN"}. Useful when cross-referencing data from
external bases (Receita Federal, DATASUS, IBGE, eSocial) against
the punctuated \code{cnpj_cia} column the package preserves from CVM.
}
\details{
\code{NA} is preserved. Inputs whose digit-only form is not exactly 14
characters return \code{NA_character_} and a single warning of class
\code{cvmdata_warn} listing the offending positions; downstream code
can decide whether to filter the NAs or escalate.
}
\examples{
cnpj_format("12345678000190")
cnpj_format(c("12345678000190", NA, "12.345.678/0001-90"))
}
\seealso{
\code{\link[=cnpj_clean]{cnpj_clean()}}

Other utilities:
\code{\link[=cnpj_clean]{cnpj_clean()}}
}
\concept{utilities}
