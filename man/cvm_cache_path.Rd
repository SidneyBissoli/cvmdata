% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cvm_cache_path}
\alias{cvm_cache_path}
\title{Path to the cvmdata cache directory}
\usage{
cvm_cache_path()
}
\value{
A character scalar with the absolute path.
}
\description{
Returns the directory where downloaded CSV/ZIP artifacts are cached.
The default is the user-level cache slot from
\code{\link[tools:userdir]{tools::R_user_dir()}}, which can be overridden by setting the
option \code{cvmdata.cache_dir} (see \code{\link[=cvm_cache_set_path]{cvm_cache_set_path()}}).
}
\details{
Cached artifacts are revalidated against the CVM portal (HEAD with
ETag/Last-Modified) only after the freshness window expires. The
window is controlled by the option \code{cvmdata.cache_ttl_seconds}
(default \code{2592000}, i.e. 30 days); set it to \code{0} to revalidate on
every call, or to \code{Inf} to skip revalidation entirely until
\code{\link[=cvm_cache_clear]{cvm_cache_clear()}} is called.

The cache size is bounded by \code{options(cvmdata.cache_max_size_mb)}
(default \code{100} MiB). Whenever the total counted across yearly
bundles and non-partitioned \verb{\{artifact, sidecar\}} pairs exceeds
90\% of the limit, the next download evicts the oldest units
(by ZIP/CSV \code{mtime}) until the cache is at or below 80\% of the
limit. Set the option to \code{0}, a negative number, or \code{Inf} to
disable eviction. Interactive sessions see a
\code{cvmdata_warn_eviction} warning after each eviction round;
batch jobs stay silent unless
\code{options(cvmdata.cache_warn_evictions = TRUE)} is set.

Read-only: this function does not create the directory.
}
\examples{
cvm_cache_path()
}
\seealso{
\code{\link[=cvm_cache_set_path]{cvm_cache_set_path()}}, \code{\link[=cvm_cache_info]{cvm_cache_info()}},
\code{\link[=cvm_cache_clear]{cvm_cache_clear()}}

Other cache: 
\code{\link{cvm_cache_clear}()},
\code{\link{cvm_cache_info}()},
\code{\link{cvm_cache_set_path}()}
}
\concept{cache}
