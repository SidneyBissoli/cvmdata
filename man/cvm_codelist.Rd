% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{cvm_codelist}
\alias{cvm_codelist}
\title{Look up the codelist for a categorical column}
\usage{
cvm_codelist(dataset, table, column, group = NULL)
}
\arguments{
\item{dataset}{Short dataset id (e.g. \code{"cad"}).}

\item{table}{Table name within the dataset (e.g. \code{"companhias"}).}

\item{column}{Snake-case column name (e.g. \code{"sit"}).}

\item{group}{Optional CKAN group slug (e.g. \code{"companhias"}). When
\code{NULL} (default) the function resolves by uniqueness across the
embedded snapshot. From v0.4 onward, datasets may exist in more
than one group; in that case omitting \code{group} aborts with
\code{cvmdata_error_input_ambiguous}.}
}
\value{
A tibble with one row per categorical value, sorted
alphabetically. Column: \code{value} (character).
}
\description{
Returns the set of categorical values observed for a single
\verb{(dataset, table, column)} triple, sourced from the snapshot
embedded under \code{inst/extdata/cvm_codelists_snapshot.csv}. The
snapshot is built offline by \code{data-raw/build-codelists-snapshot.R}
from the CVM open-data CSVs.
}
\details{
Inclusion criteria for the snapshot (see CLAUDE.md §9.2):
columns whose dictionary \code{dominio} enumerates values (e.g. \code{S/N},
\code{PF/PJ}), and \code{varchar} columns with \code{tamanho < 200} and at most
50 distinct values observed in the latest available year.
}
\examples{
cvm_codelist("cad", "companhias", "sit")
}
\seealso{
\code{\link[=cvm_dictionary]{cvm_dictionary()}}

Other discovery:
\code{\link[=cvm_dataset_years]{cvm_dataset_years()}},
\code{\link[=cvm_datasets]{cvm_datasets()}},
\code{\link[=cvm_dictionary]{cvm_dictionary()}},
\code{\link[=cvm_tables]{cvm_tables()}}
}
\concept{discovery}
