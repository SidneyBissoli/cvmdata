% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-issuer-fetch.R
\name{issuer_fetch}
\alias{issuer_fetch}
\title{Fetch a CVM open-data table for an issuer dataset}
\usage{
issuer_fetch(
  dataset,
  table,
  issuer = NULL,
  year = NULL,
  source = NULL,
  report_type = NULL,
  on_error = "abort",
  validate = "strict",
  ...
)
}
\arguments{
\item{dataset}{Short dataset id (e.g. \code{"cad"}, \code{"dfp"}).}

\item{table}{Snake-case table name (e.g. \code{"companhias"}, \code{"bpa"}).
For datasets with conceptually paired tables (individual /
consolidated), \code{table} is the concept name and \code{report_type}
selects the variant.}

\item{issuer}{Optional character vector identifying issuers to
include. Accepts CNPJ (with or without punctuation), CD_CVM (with
or without zero-padding), or free-text matched against
\code{denom_cia}. Detection is automatic per element. \code{NULL} (default)
returns every issuer. Singular naming follows tidyverse
conventions; the argument still accepts vectors of any length.
When the target table does not carry a \code{cd_cvm} column (e.g.
\code{composicao_capital}, \code{parecer}), CD_CVM tokens are resolved to
CNPJ via the dataset's \code{submissao} table for the same year — the
user-facing interface is identical regardless of which table
holds CD_CVM natively.}

\item{year}{Integer vector of years to fetch. \code{NULL} (default)
fetches the latest available year. Ignored for datasets with
\code{temporal_partitioning: none} (e.g. CAD). Singular naming follows
tidyverse conventions; the argument still accepts vectors of any
length.}

\item{source}{One of \code{"mirror"} (parquet via DuckDB) or \code{"cvm"}
(CVM Open Data Portal). \code{NULL} (default) resolves to the active
backend via \code{\link[=cvm_source_get]{cvm_source_get()}} — \code{"mirror"} from v0.1.0 onward
unless the user has flipped it via
\code{cvm_source_set("cvm")}.}

\item{report_type}{One of \code{"ind"}, \code{"con"}, or \code{NULL}. Required for
tables that publish individual and consolidated variants
(\code{bpa}, \code{bpp}, \code{dre}, \code{dra}, \code{dfc_md}, \code{dfc_mi}, \code{dmpl}, \code{dva}).
Must be \code{NULL} for tables without that distinction
(\code{composicao_capital}, \code{submissao}, \code{parecer}, \code{companhias}).}

\item{on_error}{One of \code{"abort"} (default), \code{"warn"} or \code{"silent"}.
Controls behaviour on HTTP failures for batches of yearly tables
(\code{year = c(...)} with more than one element). With \code{"warn"}, the
failed years are skipped and a \code{cvmdata_warn_partial_failure}
warning lists them; with \code{"silent"}, the failed years are skipped
silently; with \code{"abort"}, the first failure aborts the call. Total
batch failure (every year fails) always aborts regardless of the
setting — there is no partial result to return. Parse/validation
failures are governed by \code{validate}, not \code{on_error}. Single-year
calls, non-yearly tables, and the implicit fallback when
\code{year = NULL} always abort on HTTP failure.}

\item{validate}{One of \code{"strict"} (default), \code{"warn"} or \code{"skip"}.
Controls schema validation strictness at parse time.}

\item{...}{Reserved for forward compatibility. Currently no extra
arguments are accepted; passing any aborts with
\code{cvmdata_error_input}.}
}
\value{
A tibble of class \code{cvm_tbl} carrying the five provenance
attributes \code{source}, \code{fetched_at}, \code{dataset}, \code{table} and
\code{package_version}.
}
\description{
Single entry point for retrieving any table from any CVM
issuer-class dataset covered by the package. Selects the right URL,
downloads (or serves from cache), parses, applies schema-declared
transformations, and returns a tibble carrying provenance
attributes.
}
\details{
Column names, categorical values and free-text fields are preserved
in Portuguese exactly as published by CVM (snake_case minúsculo).
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# CAD: single snapshot, no temporal partitioning, no report_type
issuers <- issuer_fetch("cad", "companhias")

# DFP BPA individual, latest year, single issuer
bb <- issuer_fetch("dfp", "bpa",
                   report_type = "ind",
                   issuer = "BCO BRASIL",
                   year = 2024)
\dontshow{\}) # examplesIf}
}
\concept{fetchers}
