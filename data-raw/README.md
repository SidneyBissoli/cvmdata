# data-raw

Scripts used to build artefacts under `inst/extdata/` — schema YAMLs,
the CVM dictionary snapshot (`cvm_dictionary_snapshot.csv`), the
empirical codelists snapshot (`cvm_codelists_snapshot.csv`) and the
hash inventories used by the periodic update workflow.

This directory is excluded from the package tarball via `.Rbuildignore`.
